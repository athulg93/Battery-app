<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Normal app theme -->
    <style name="Theme.BatteryMonitor" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
    </style>

    <!-- Splash Screen theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, icon and animated duration -->
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_volt_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.BatteryMonitor</item>
    </style>
</resources>
